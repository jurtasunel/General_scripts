#!/bin/bash

# Format disks to have NTFS (Windows compatible and files biger tan 4GB) instead of FAT32(old version).
# 1. Empty disk.
# 2. Go to disks > stop dist > configure > check file system to see format.
# 3. Format volume > Choose option "For use with Windows (NTFS)

# Decompress tar files.
#tar -xf {file_name}
